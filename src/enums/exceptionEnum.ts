export enum EException {
  PAGE_NOT_ACCESS = 403,
  PAGE_NOT_FOUND = 404,
  SERVICE_ERROR = 500,
}

export enum EErrorType {
  VUE = 'vue',
  SCRIPT = 'script',
  RESOURCE = 'resource',
  AJAX = 'ajax',
  PROMISE = 'promise',
}
